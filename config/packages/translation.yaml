framework:
    default_locale: fr
    # Keep in step with App\Enum\AppLocale.
    enabled_locales: ['fr', 'en', 'es', 'pt', 'de']
    translator:
        default_path: '%kernel.project_dir%/translations'
        fallbacks:
            - fr
