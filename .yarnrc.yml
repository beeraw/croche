# Webpack Encore, Babel and Playwright all expect to find their packages on
# disk, so the classic layout stays. Plug'n'Play would be stricter and faster,
# but it asks for patches this project has no reason to carry.
nodeLinker: node-modules
